#include <catch2/catch_test_macros.hpp>

#include <opencv2/core.hpp>

//#include "openlib/vision/image_ops.hpp"

// TEST_CASE("to_gray converts 3-channel to 1-channel") {
//   cv::Mat bgr(10, 10, CV_8UC3, cv::Scalar(10, 20, 30));
//   cv::Mat gray = openlib::vision::to_gray(bgr);
//   REQUIRE(gray.rows == 10);
//   REQUIRE(gray.cols == 10);
//   REQUIRE(gray.channels() == 1);
// }
